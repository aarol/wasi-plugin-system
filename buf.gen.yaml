version: v1

managed:
  enabled: true
  go_package_prefix:
    default: github.com/aarol/wasi-plugin-system/gen/plugin

plugins:
  - plugin: buf.build/protocolbuffers/go
    out: gen/plugin
    opt: paths=source_relative
